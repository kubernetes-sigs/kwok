name: Test

on:
  pull_request:
    paths:
    - pkg/**/*
    - cmd/**/*
    - test/**/*
    - hack/**/*
    - kustomize/**/*
    - go.mod
    - .github/workflows/test.yaml
    - '!hack/releases-helm-chart.sh'
  push:
    paths:
    - pkg/**/*
    - cmd/**/*
    - test/**/*
    - hack/**/*
    - kustomize/**/*
    - go.mod
    - .github/workflows/test.yaml
    - '!hack/releases-helm-chart.sh'

env:
  DOCKER_CLI_EXPERIMENTAL: "enabled"
  CGO_ENABLED: "0"
  NERDCTL_VERSION: "2.1.2"

# https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#concurrency
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test-build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
        - os: ubuntu-latest
          builder: docker
        - os: ubuntu-latest
          builder: podman
        - os: ubuntu-22.04
          builder: nerdctl
    steps:
    - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
    - name: Set up Go
      uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
      with:
        go-version-file: go.mod

    - name: Test
      shell: bash
      run: |
        ./hack/e2e-test.sh release/build

    - name: Build
      shell: bash
      run: |
        make build

    - name: Install Nerdctl and Start Containerd
      if: ${{ matrix.builder == 'nerdctl' }}
      shell: bash
      run: |
        curl -sSL https://github.com/containerd/nerdctl/releases/download/v${{ env.NERDCTL_VERSION }}/nerdctl-full-${{ env.NERDCTL_VERSION }}-linux-amd64.tar.gz -o - | sudo tar -xz -C /usr/local
        containerd-rootless-setuptool.sh install
        containerd-rootless-setuptool.sh install-buildkit-containerd

    - name: Build Image
      shell: bash
      run: |
        BUILDER=${{ matrix.builder }} make build-image

    - name: Build Cluster Image
      shell: bash
      run: |
        BUILDER=${{ matrix.builder }} make build-cluster-image

  test-kwok-e2e:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
    - name: Set up Go
      uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
      with:
        go-version-file: go.mod

    - name: Test e2e default
      shell: bash
      run: |
        ./hack/e2e-test.sh e2e/kwok/default

    - name: Test e2e single
      shell: bash
      run: |
        ./hack/e2e-test.sh e2e/kwok/single

  test-kwok:
    strategy:
      fail-fast: false
      matrix:
        case:
        - kwok
    continue-on-error: false
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
    - name: Set up Go
      uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
      with:
        go-version-file: go.mod

    - name: Test Workable
      shell: bash
      run: |
        ./hack/e2e-test.sh ${{ matrix.case }}/${{ matrix.case }}

  test-kwokctl:
    # https://docs.github.com/en/actions/using-jobs/using-a-matrix-for-your-jobs
    strategy:
      fail-fast: false
      matrix:
        # https://docs.github.com/en/actions/using-github-hosted-runners/about-github-hosted-runners/about-github-hosted-runners#supported-runners-and-hardware-resources
        include:
        # Windows
        # Linux containers are not supported on Windows Runner
        # https://github.com/orgs/community/discussions/25491#discussioncomment-3248089
        - os: windows-latest
          kwokctl-runtime: binary

        # MacOS amd64
        - os: macos-15-intel
          kwokctl-runtime: binary
        # MacOS arm64
        - os: macos-15
          kwokctl-runtime: binary

        # Linux
        - os: ubuntu-latest
          kwokctl-runtime: binary
        - os: ubuntu-latest
          kwokctl-runtime: docker
        - os: ubuntu-latest
          kwokctl-runtime: podman
        - os: ubuntu-latest
          kwokctl-runtime: kind
        - os: ubuntu-22.04
          kwokctl-runtime: nerdctl

    continue-on-error: false
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
    - name: Set up Go
      uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
      with:
        go-version-file: go.mod

    - name: Install CoreUtils for MacOS
      if: ${{ startsWith(matrix.os, 'macos-') }}
      shell: bash
      run: |
        brew install coreutils bash

    - name: Install Nerdctl and Start Containerd
      if: ${{ matrix.kwokctl-runtime == 'nerdctl' }}
      shell: bash
      run: |
        curl -sSL https://github.com/containerd/nerdctl/releases/download/v${{ env.NERDCTL_VERSION }}/nerdctl-full-${{ env.NERDCTL_VERSION }}-linux-amd64.tar.gz -o - | sudo tar -xz -C /usr/local
        containerd-rootless-setuptool.sh install
        containerd-rootless-setuptool.sh install-buildkit-containerd

    - name: Install kubectl
      shell: bash
      run: |
        ./hack/requirements.sh kubectl

    - name: Install kind
      if: ${{ matrix.kwokctl-runtime == 'kind' }}
      shell: bash
      run: |
        ./hack/requirements.sh kind

    - name: Test e2e dry run
      if: ${{ matrix.os == 'ubuntu-latest' && matrix.kwokctl-runtime == 'binary' }}
      shell: bash
      run: |
        PATH=$(pwd)/bin:${PATH} ./hack/e2e-test.sh e2e/kwokctl/dryrun

    - name: Test e2e
      shell: bash
      run: |
        PATH=$(pwd)/bin:${PATH} ./hack/e2e-test.sh e2e/kwokctl/${{ matrix.kwokctl-runtime }} && rm -rf logs

    - name: Test Workable
      shell: bash
      run: |
        if [[ "${{ matrix.os }}" == "ubuntu-latest" && ( "${{ matrix.kwokctl-runtime }}" == "binary" || "${{ matrix.kwokctl-runtime }}" == "docker" ) ]]; then
          export LAST_RELEASE_SIZE=24
        fi
        PATH=$(pwd)/bin:${PATH} ./hack/e2e-test.sh kwokctl/kwokctl_${{ matrix.kwokctl-runtime }}

    - name: Test PodGroup
      if: ${{ matrix.kwokctl-runtime == 'docker' }}
      shell: bash
      run: |
        PATH=$(pwd)/bin:${PATH} ./hack/e2e-test.sh kwokctl/kwokctl_podgroup

    - name: Test Kueue
      if: ${{ matrix.kwokctl-runtime == 'docker' }}
      shell: bash
      run: |
        PATH=$(pwd)/bin:${PATH} ./hack/e2e-test.sh kwokctl/kwokctl_kueue

    - name: Test JobSet
      if: ${{ matrix.kwokctl-runtime == 'docker' }}
      shell: bash
      run: |
        PATH=$(pwd)/bin:${PATH} ./hack/e2e-test.sh kwokctl/kwokctl_jobset

    - name: Test LWS
      if: ${{ matrix.kwokctl-runtime == 'docker' }}
      shell: bash
      run: |
        PATH=$(pwd)/bin:${PATH} ./hack/e2e-test.sh kwokctl/kwokctl_lws

    - name: Test Descheduler
      if: ${{ matrix.kwokctl-runtime == 'docker' }}
      shell: bash
      run: |
        PATH=$(pwd)/bin:${PATH} ./hack/e2e-test.sh kwokctl/kwokctl_descheduler

    - name: Upload logs
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
      if: failure()
      with:
        name: kwok-logs-${{ github.run_id }}-${{ matrix.os }}-${{ matrix.kwokctl-runtime }}
        path: ${{ github.workspace }}/logs
